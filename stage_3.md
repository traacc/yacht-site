# Этап 3 — задачи и прогресс

Источник: `import_data/Детализация_ТЗ_3го_этапа_сайтас_корректировками.docx`.
Документ не финальный — заканчивается фразой «Продолжение чуть позже», финансовый блок
помечен как «будет дополняться в процессе коммерческого блока».

Статус на 30.07.2026. Легенда: ✅ сделано · 🟡 частично · ⬜ не начато · ❓ требует уточнения у заказчика.

Сводка: **1. Общее** 🟡 · **2. Верификация и уведомления** 🟡 · **3. Результаты** ✅ ·
**4. Финансы** 🟡 · **5. Carter 30** ✅ · **6. Помощь** ✅ · **7. Услуги** 🟡 (каркас,
аренда флота, обучение и мероприятия готовы) · **8. Биржи** 🟡 · **9. Главная** ⬜

---

## 1. Общее

### 1.1. Обработка загружаемых фото 🟡

Требование: сжатие пользовательских загрузок до 50–70 кБ (админам — без ограничений),
поддержка HEIC, обрезка, единое соотношение сторон.

- ✅ HEIC/HEIF: декодирование через Imagick + libheif/libde265, нормализация в JPEG/WebP
  на загрузке — `app/Services/ImageConverter.php`, трейт `app/Models/Concerns/NormalizesHeicImageColumns.php`.
- ✅ Адаптивные форматы WebP/AVIF в медиаколлекциях (Spatie Media Library).
- ✅ Обрезка и фиксированные соотношения сторон: `->imageEditor()` + `imageEditorAspectRatios()`
  в ресурсах News, Teams, Users, Helps, Gallery.
- ⬜ Целевой вес 50–70 кБ не задан: сейчас качество фиксировано (`quality = 82/85`),
  ограничение по итоговому размеру файла и разделение лимитов «пользователь / админ» отсутствуют.

### 1.2. Всплывающие подсказки «?» ⬜❓

Тултипы у кнопок/ссылок и знак вопроса вверху справа на страницах.
Единого механизма нет — только точечные `->helperText()`/`->tooltip()` в админке.
Заказчик обещал определить конкретные места размещения.

---

## 2. Верификация и уведомления пользователей

### 2.1. Верификация email ✅

Подтверждение email при самостоятельной регистрации: `User implements MustVerifyEmail`,
`app/Actions/Auth/SendEmailVerificationLinkAction.php`, баннер
`app/Livewire/EmailVerificationBanner.php`, подписанные ссылки с `throttle:6,1` (`routes/auth.php`).

### 2.2. Верификация телефона ⬜

В `users` есть колонка `phone_verified_at`, но флоу отправки и проверки кода нет,
SMS-провайдер не подключён.

### 2.3. Вход через Сбер ID / Альфа ID / Госуслуги / Мос.ру ⬜❓

Не начато. Требуется отдельно выяснить техническую возможность и порядок подключения
(для Госуслуг — регистрация ИС в ЕСИА). Плюс сценарий привязки к автосозданным аккаунтам
после сверки ФИО и даты рождения.

### 2.4. Защита от ботов 🟡

- ✅ Rate limiting на чувствительных маршрутах (`throttle:5,1` на вопросах,
  `throttle:6,1` на верификации, `throttle:10,1` на отписке).
- 🟡 Правило `app/Rules/YandexCaptcha.php` написано, но подключение закомментировано
  (`routes/web.php:881`).
- ⬜ Лимиты на количество запросов кода на телефон/почту — после появления SMS-верификации.

### 2.5. Центр уведомлений в ЛК ✅

- Модель `NotificationPreference` (строка на пару «категория + канал»); отсутствие строки
  означает «включено» — все ранее зарегистрированные пользователи по умолчанию получают всё,
  как требует ТЗ.
- Категории (`App\Enums\NotificationCategory`): запросы с сайта, важная информация,
  сообщения чата, новости/анонсы.
- Каналы (`App\Enums\NotificationChannel`): email, Telegram, database, **MAX** (заведён в enum).
- Чекбоксы при регистрации — `app/Actions/Notifications/ApplyRegistrationPreferencesAction.php`.
- Telegram: привязка аккаунта по токену, обработка блокировки бота
  (`TelegramAccount`, `TelegramLinkToken`, `app/Services/Telegram/`).
- Отписка по подписанной ссылке из письма — `notifications.unsubscribe`.
- Брендированные письма с логотипом и футером, обложки в новостных рассылках.

---

## 3. Автоматизация результатов ✅ (кроме судейской программы)

### 3.1. Внедрение судейской программы 🟡❓

- ✅ Внешний API для интеграции: `routes/api.php`, токены в SiteSettings, OpenAPI-спека
  (`docs/openapi.yaml`, `docs/external-api.md`), команда smoke-проверки эндпоинтов.
- ✅ `external_id` у регат для сопоставления с судейской программой.
- ✅ Импорт протоколов: `ImportRegattaResultItemsAction`, `ImportRgdResultItemsAction`,
  парсер RGD (`app/Services/Rgd/`), выгрузка участников `RgdParticipantsExporter`.
- ⬜ Запуск самой программы внутри платформы и её облачный компонент — вопрос к заказчику
  (какая программа, есть ли API/облако).

### 3.2. Выброс худших результатов ✅

- Поля регаты `discards_count`, `discard_1_after_races`, `discard_2_after_races`
  (миграция `2026_07_24_000001`), настраиваются при создании регаты.
- Дефолты по ТЗ: 1 выброс с 6 гонок, 2 выброса — с 9 (`discardsToApply()`).
- Флаг `race_results.is_discarded`, выброшенные результаты отображаются в скобках.
- Пересчёт идемпотентный, при смене настройки регаты суммы и места пересчитываются
  (`app/Models/Regatta.php:143`).

### 3.3. Алгоритм итогового места ✅

`RegattaResultResource::recomputeItemTotals()` / `assignPositions()`:
сумма очков минус выбросы → ранг по возрастанию → тай-брейк по отсортированному ряду
зачётных очков → при полном равенстве результат последней проведённой гонки.
Ручные значения (`*_overridden`) авторасчётом не затираются.

---

## 4. Финансовый блок 🟡

### 4.1. Платежи и эквайринг 🟡

- ✅ Справочник назначений `App\Enums\PaymentPurpose`: членский взнос, стартовый взнос,
  ужин, спонсорский взнос, аренда яхты, мероприятие, реклама, прочее.
- ✅ Инфраструктура платежей: `PaymentTransaction`, `app/Services/Payments/`
  (`PaymentGateway`, `PaymentManager`, DTO), приём вебхуков (`HandleWebhookAction`),
  возврат на сайт (`payments.return`).
- 🟡 Реализован только `TestPaymentProvider` (симулятор). ⬜ Боевой эквайринг не подключён —
  по ТЗ нужно передать заказчику инструкцию по требуемым документам.
- ⬜ Условие «финансовые операции только для верифицированных» — не проверяется (зависит от 2.2).
- ⬜ Установка размеров взносов админом в профильных разделах
  («Правила вступления» — членский взнос, «Реклама и спонсоры» — реклама).
- ⬜ Проверка платежа на соответствие условиям события.

### 4.2. Реестр платежей ✅

`PaymentRegistry` + `PaymentRegistryResource`:
- подтверждение прихода бухгалтером (`ConfirmPaymentRegistryAction`);
- нал/безнал (`PaymentMethod`, `PaymentSettlement`), ручное внесение наличных;
- полный лог изменений `PaymentRegistryLog` + `PaymentRegistryLogger`, колонка
  «последнее изменение» (кто акцептовал/менял);
- выгрузка реестра и лога в Excel — `app/Exports/PaymentRegistryExport.php`,
  `PaymentRegistryLogExport.php` (доступ — админ и бухгалтер).

### 4.3. Группировка и фильтры ✅

Миграция `2026_07_30_000001`: денормализованные `regatta_id`, `yacht_id`, `team_id`,
`purpose`, `payer_name` + индексы под сценарий ТЗ «по такой-то яхте на такой регате
в такую-то дату» (`SyncPaymentRegistryLinksAction`). В таблице — фильтры по регате,
яхте, команде, назначению, способу оплаты, дате и автосуммирование (`Sum`, `Count`).

### 4.4. Реестр расходов ⬜

Текущая модель `Expense` — это только «название + прикреплённый документ»
(наследие 2-го этапа). Требуемых полей ТЗ (дата, сумма, нал/безнал, группа расходов
из справочника, назначение) нет. Нужна новая структура + справочник групп расходов.

### 4.5. Финансовый отчёт за период ⬜

`FinancialReport` — тоже только загружаемый файл. Генерации отчёта
(все приходы «от кого и за что», все расходы, итог), отчёта по расходам за период
с выбором 1–4 групп и отдельного учёта приходов по разделу «Услуги» — нет.

### 4.6. Интеграция бухгалтерской программы ⬜❓

Не начато; по ТЗ — «попробовать», при невозможности ограничиться автоматизацией приходов.

---

## 5. Новый раздел «Carter 30» ✅

Все шесть подразделов готовы. Раздел в меню сайта (`nav`, футер), страницы в `sitemap.xml`,
весь контент правится из админки (группа «Сайт»).

- ✅ **История** — `/carter30/history`, настройка `carter30.history`
  (`Carter30HistoryPageSettings`, RichEditor с вложениями — картинки в теле текста).
- ✅ **Технический регламент класса** — `/carter30/regulations`. Источник контента общий с
  `/association/regulations` (настройки `regulations.*`), тело страницы —
  `resources/views/partials/regulations-content.blade.php`. Захардкоженные ранее в blade
  «Основные положения» переехали в настройку `regulations.provisions`
  (data-миграция `2026_07_31_000003`).
- ✅ **Ремонт и модернизация** — `/carter30/repair` (интро + чертежи из
  `Carter30RepairPageSettings`) и кейсы `/carter30/repair/{slug}` (модель `RepairCase`,
  `RepairCaseResource`): текст с картинками, чертежи, фото и видео-ссылки с подписями,
  лайтбокс. Подписи к фото хранятся в `Media::name`.
- ✅ **Кнопка «Хотите такой ремонт?»** — `SubmitRepairRequestAction`: письмо на адрес из
  настройки `site.order_email` (дефолт `order@carter-pro.ru`) с темой, называющей кейс,
  + колокольчик в админке + модель `RepairRequest` / `RepairRequestResource`.
- ✅ **Техническая помощь** — `/carter30/technical-help`. Справочник специалистов
  (`Help`/`HelpCategory`) вынесен в `App\Services\HelpDirectory` и партиал
  `partials/help-directory.blade.php`; вкладка «Для владельцев яхт» на `/help` осталась —
  контент один, входа два.
- ✅ **Барахолка** (`/carter30/marketplace`) и **Продать яхту** (`/carter30/yachts-for-sale`) —
  единая подсистема `Advert` (`AdvertType`, `AdvertStatus`, `AdvertCategory`), спроектированная
  под все шесть досок:
  - подача и управление в ЛК (`User\Resources\Adverts`): фото до 10 шт., цена + «договорная»,
    город, категория/яхта, контакты для публикации с валидацией (телефон по маске +7,
    Telegram `@nickname`, email);
  - премодерация: `SubmitAdvertAction` шлёт письмо на `adminNotificationEmails()` **и**
    колокольчик модераторам; `ModerateAdvertAction` уведомляет автора о решении
    (`AdvertModeratedNotification`, категория `SiteRequests`); причина отказа видна автору;
  - правка опубликованного объявления возвращает его на модерацию;
  - статусы автора: «Продано» (остаётся на витрине с плашкой), «Снять», «Опубликовать снова»;
  - витрина с **серверными** фильтрами (`App\Services\AdvertBoard`): поиск, категория, город,
    диапазон цены, сортировка, пагинация с `withQueryString()`.

---

## 6. Раздел «Помощь» ✅

- ✅ Помощь по сайту: `Help` + `HelpCategory`, страница `/help`.
- ✅ F.A.Q. с аккордеоном и формой «Задать вопрос»: `Faq`, `UserQuestion`,
  `SubmitUserQuestionAction`, уведомление в админпанель и на почту.
- ✅ Онлайн-чат поддержки внутри сайта: `Conversation`, `ConversationParticipant`,
  `ChatMessage`, `app/Livewire/Chat/` (виджет + тред), `app/Actions/Chat/`,
  вложения. Enum `ConversationType` уже предусматривает личную переписку (`direct`) —
  задел под биржи матросов.

---

## 7. Новый раздел «Услуги» 🟡

Раздел заведён: хаб `/services`, пункт в меню и футере, страницы в `sitemap.xml`, весь контент
правится из админки (страница «Услуги» в группе «Сайт», вкладки по подразделам).

**Единая подсистема заявок** — модель `ServiceRequest` (одна таблица на все подразделы:
`ServiceType` + специфичные поля в json `payload`), спроектированная так же, как `Advert`
покрывает шесть досок объявлений:
- `SubmitServiceRequestAction` закрывает все три адресата ТЗ: письмо на `site.order_email`
  с темой, называющей услугу и параметры («Заявка на услугу: Аренда флота (01.06–05.06,
  4 яхты)»), колокольчик модераторам и счётчик на дашборде;
- поля формы, обязательность email, диапазон дат и подпись количества задаёт
  `ServiceType` — новый подраздел добавляется кейсом enum;
- `payload` чистится по белому списку полей подраздела;
- `ServiceRequestResource` (админка): фильтры по услуге и статусу, статусы
  «Новая / В работе / Выполнена / Отклонена» + `processed_at`/`processed_by`, детали
  заявки из `payload` в карточке;
- виджет `RequestsOverview` на дашборде — заявки на услуги, аренду и ремонт;
- общий blade-компонент `<x-service-request-button>` — одна Alpine-модалка на все
  подразделы, поля рендерятся из `ServiceType::payloadFields()`;
- в `SiteSettings` появилось поле «E-mail отдела заказов» (раньше `site.order_email`
  только читался, изменить его из админки было нельзя).

| Подраздел | Статус | Комментарий |
|---|---|---|
| Аренда яхт | 🟡 | Есть `YachtRental` (календарь занятости с ценами) и `YachtRentalRequest` + `RentalRequests` в админке, форма запроса на сайте (`yacht-rental.request`); из раздела «Услуги» ведёт ссылка на `/yachts`. Нет: витрины бронирования по образцу boataround.com, онлайн-оплаты после подтверждения, пользовательского соглашения с галочкой |
| Аренда флота | ✅ | `/services/fleet-rental`: серверный подбор «N яхт на диапазон дат» (`App\Services\FleetAvailability`) по календарю аренды, одобренным бронированиям и занятости регатами; ориентировочная стоимость, карточки свободных яхт, заявка с предзаполненными датами и количеством |
| Проведение мероприятий | 🟡 | `/services/events`: текстовый блок, форматы, флот (яхты `for_rent` — общий источник с `/yachts`), площадки, галерея ивентов, проведённые мероприятия, заявка. ⬜ «Конструктор мероприятия» (11 параметров, автоподбор яхт, расчёт минимальной стоимости) — ❓алгоритмы с заказчиком не согласованы; на странице оставлено место под секцию. ⬜ SEO/AI-оптимизация |
| Обучение судовождению | ✅ | `/services/training`: текст, программы (длительность/стоимость), галерея, форма заявки со всеми полями ТЗ — ФИО, телефон, обязательный email, чему учиться, права IYT/ГИМС, количество человек |
| Яхтенные путешествия и походы | ⬜ | Туры в админке (даты, маршрут, «на чём», цены за место/каюту, оргсбор, галерея) + заявка на участие. Кейс `ServiceType::Tour` заведён |
| Регаты за рубежом | ⬜ | Карточки регат с попаданием в общий календарь сезона, список яхт под аренду с занятостью, заявка на участие. Кейс `ServiceType::ForeignRegatta` заведён |
| Подарочные сертификаты | ⬜ | Каталог сертификатов (фиксированная или диапазонная цена), заказ со статусами «оплачен» и «услуга оказана». Кейс `ServiceType::GiftCertificate` заведён; статусы добавляются в `ServiceRequestStatus` без миграции |

Онлайн-оплата услуг не подключалась (ждёт выбора эквайринга, п. 4.1): в `service_requests`
заведена nullable-связь `payment_registry_id` без логики.

---

## 8. Раздел «Соревнования» — биржи 🟡

Ядро подсистемы объявлений готово (см. п. 5): модель `Advert` с премодерацией, фото,
контактами и витриной, а также канал «запрос → автор» через `ConversationType::Direct`
(`StartDirectConversationAction`, страница «Сообщения» в ЛК, уведомления). Каждая биржа
сводится к кейсу в `AdvertType` + своим полям, форме и витрине.

Что уже сделано под биржи в чате:
- ветка `Direct` в `SendChatMessageAction::resolveRecipients()` — без неё сообщение по
  объявлению уходило всем операторам поддержки, а адресат не получал ничего;
- `ChatMessageReceivedNotification` переведён с булева `forSupport` на
  `ChatNotificationAudience` (три режима) + deep-link `?conversation=`;
- «своё/чужое» в ленте определяется по автору сообщения, а не по роли — в Direct обе
  стороны пишут с ролью `Client`;
- `ChatAttachments::allows()` больше не даёт операторам доступ к вложениям личных переписок.

### 8.1. Порядок подразделов ⬜

Требуемый порядок: Календарь → Серии → Заявки → Результаты → Биржа шкиперов и матросов →
Биржа парусов → Экипажи для соревнований → Яхты для соревнований.

### 8.2. Четыре доски объявлений ⬜

Не начаты. Общие требования ко всем: только для пользователей с ЛК, ФИО подставляется из ЛК,
контакты для публикации необязательны (телефон по маске +7, Telegram `@nickname`,
email с валидацией), кнопка «Отправить запрос» → в ЛК автора + уведомление на его email.

- **Биржа шкиперов и матросов** — предложения услуг и запросы на поиск; позиция
  (рулевой/матрос/любая), разряд (3, 2, 1, КМС…), стоимость (руб./час или руб./день),
  до 5 фото.
- **Экипажи для соревнований** — экипажи без яхт ищут лодку; описание экипажа, стоимость
  услуг, описание запроса, до 10 фото.
- **Яхты для соревнований** — владельцы ищут экипаж; выбор зарегистрированной яхты с переходом
  в карточку, выбор нескольких регат, до 10 фото.
- **Биржа парусов** — продажа/аренда парусов; цена (руб. или руб./день), залог при аренде,
  до 10 фото; запросы — до 5 фото.

**Рекомендация (принята и реализована в п. 5):** все четыре биржи + «Барахолка» и «Продать
яхту» — одна модель «объявление с премодерацией и запросом в ЛК». Осталось: у бирж есть
дуальность «предложение / запрос» (два набора полей внутри одной доски у шкиперов и парусов),
связи с регатами и датами («Когда»), поле «На какую яхту» с тройственной семантикой
(FK / пусто / свободный текст) и лимит фото 5 либо 10 в зависимости от доски.

❓ Уточнить у заказчика: применяется ли премодерация к четырём биржам — в ТЗ она прописана
только для «Барахолки» и «Продать яхту».

---

## 9. Главная страница ⬜

- **«Новости парусного мира»** (под новостями ассоциации) — AI-движок поиска и публикации
  новостей парусного спорта, промпты редактируются в админке, публикация = ссылка + сама
  статья. ❓Критерии отбора и формат заказчик обсудит после готовности движка.
- **«Пресса о нас»** — статьи сторонних изданий об ассоциации и соревнованиях Carter 30
  (в админке ссылка → на сайте ссылка и текст статьи).

---

## Открытые вопросы к заказчику

1. Места размещения подсказок «?» (п. 1.2).
2. Техническая возможность и договорённости по Сбер ID / Альфа ID / Госуслуги / Мос.ру (п. 2.3).
3. Какая судейская программа внедряется, есть ли у неё API и облачный компонент (п. 3.1).
4. Эквайринг: выбор провайдера и пакет документов (п. 4.1).
5. Алгоритмы подбора яхт и расчёта минимальной стоимости в конструкторе мероприятий (п. 7).
6. Критерии отбора и формат публикации AI-новостей (п. 9).
7. Продолжение ТЗ, обещанное в конце документа, и дополнения по коммерческому блоку.

## Оценка приоритетов

Самые трудоёмкие блоки — **финансовый** (боевой эквайринг, реестр расходов, отчётность)
и остаток **«Услуг»** (бронирование яхт, конструктор мероприятий, туры, зарубежные регаты,
сертификаты). Каркас «Услуг» с единой моделью `ServiceRequest` уже есть, поэтому оставшиеся
подразделы сводятся к своей модели контента + кейсу в `ServiceType`.
Биржи дешевле делать одной подсистемой (см. п. 8.2) — вместе с «Барахолкой» и «Продать яхту»
из п. 5. Быстро реализуется «Пресса о нас».
